# verifier-cli

Independent chain + timestamp verifier. Deliberately a separate codebase (Rust or Go recommended) with no shared dependencies on the rest of this repo, so a third party who doesn't trust the main Sentrix codebase can still verify the evidence chain.

Status: not yet implemented — see `docs/ROADMAP.md` for the tier this belongs to.
