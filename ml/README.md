# ml

Model training (Random Forest / XGBoost), SHAP explainability, and versioned benchmark reports. See benchmarks/ for the (currently empty) honest benchmark history.

Status: not yet implemented — see `docs/ROADMAP.md` for the tier this belongs to.
